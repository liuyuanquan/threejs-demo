// ==================== 传递变量 ====================
varying vec2 vUv;  // 用于将UV坐标传递给片段着色器

// ==================== 主函数 ====================
void main() {
	// 将UV坐标传递给片段着色器
	vUv = uv;
	
	// 设置顶点位置（直接使用模型坐标，不做变换）
	gl_Position = vec4(position, 1.0);
}